# 地震道集标注器 — 计划与进度文档

> 本文档是项目的唯一权威计划，随开发持续更新。每次改动计划前先改这里。

## 1. 项目目标

桌面可视化程序，用于标注地震道集（共炮集 / 共检波点道集 / 共中心点道集 / 残差等）的特征，
产出「道集 ↔ 标签 ↔ 自然语言句子 ↔ 图片」的 JSONL 数据集，供后续分类模型与多模态模型训练。

## 2. 已确认的决策（与用户商定的结论）

| 项目 | 结论 |
|---|---|
| 技术栈 | Python 3.10（conda env `seismic`）+ PyQt5 + numpy memmap + matplotlib |
| SEG-Y 读取 | 自实现读取层，支持大端/**小端**（示例文件为小端，segyio 无法读取），支持自动检测 |
| 道头字节编号 | **1-based**（SEG-Y 标准文档惯例），如 95-96 指第 95~96 字节 |
| 抽道集 | 自动列出键道头所有唯一值供勾选 + 支持手动输入值（两者都要） |
| clip | 按分位数 clip（界面可调） |
| 配色 | seismic 红-白-蓝 |
| 图片 | 标注时自动导出 PNG，路径写入 JSONL |
| 结果存储 | JSONL（含 sentence 字段） |
| 界面语言 | 中文 |
| 交互 | 键盘快捷键、可返回修改、支持跳过、断点续标 |
| 标签体系 | 配置文件驱动（label_config.yaml），先按初版清单实现，后续迭代 |

## 3. 测试数据

- 路径：`/data/liuqi/code/data/shiji/01SB21_DenoiseB4_FFID19037.sgy`
- 实测结构：**小端序**，格式码 5（float32），19008 道 × 4000 采样点，dt=2ms
- 道头分布：9-12 仅 1 个值（FFID=19037）；**95-96 有 36 个唯一值（1373~1793 步长 12，应为炮号，每炮 528 道）**；13-16 为道号 1~19008
- 典型用法验证：按 95-96 抽炮集、按 13-16 排序 → 36 个炮集 ✔

## 4. 架构设计

```
seismic_biaozhu/
├── PLAN.md                # 本文档：计划与进度
├── MEMORY.md              # 版本迭代记录
├── label_config.yaml      # 标签体系与句子模板（可配置）
├── segy_reader.py         # SEG-Y 读取层（端序检测、任意字节道头、memmap）
├── gather.py              # 多级排序 + 抽道集
├── preprocess.py          # 预处理管线（注册表模式，初版：分位数 clip）
├── imaging.py             # 道集 → seismic 配色 PNG
├── labels.py              # 标签配置加载 + 句子模板渲染
├── storage.py             # JSONL 读写 + 断点续标
└── main_window.py         # PyQt5 向导式界面（三步：文件→预处理→标注）
```

### 初版标签清单（label_config.yaml，可改）

- 集合类型：炮集 / CMP道集 / 共检波点道集 / 残差 / 其他
- 静校正：需要 / 不需要
- 面波噪声：存在 / 不存在 / 已压制但有残留
- 随机噪声：存在 / 不存在 / 已压制但有残留
- 工业噪声：存在 / 不存在 / 已压制但有残留
- 每个特征附「不确定」选项
- 句子模板：`这是一条{集合类型}，{静校正}静校正，面波噪声{面波噪声}，随机噪声{随机噪声}，工业噪声{工业噪声}。`

### 输出结构

```
输出目录/
├── labels.jsonl          # 每行：gather_id/键值/各特征标签/sentence/image_path/source_file
└── images/               # shot_1373.png ...
```

## 5. 进度看板

- [x] 需求确认（技术栈、字节基准、交互、存储等 8 项决策）
- [x] 示例文件诊断（发现小端序 → 决定自实现读取层）
- [x] 建立 PLAN.md / MEMORY.md
- [x] **步骤 1**：segy_reader.py 读取层 + 用示例文件验证（端序自动检测为 little ✔ 36 炮 ✔ 道数据读取 ✔）
- [x] **步骤 2**：gather.py 排序/抽道集 + preprocess.py clip + imaging.py 出图（炮集 1373 验证图已生成并给用户确认：36 炮 ✔ 99% clip ✔ 中文渲染 ✔）
- [x] **步骤 3**：label_config.yaml + labels.py + storage.py（句子渲染 ✔ 缺项校验 ✔ JSONL 读写/修改/断点续标 ✔）
- [x] **步骤 4**：main_window.py PyQt5 界面整合（离屏端到端测试全通过，截图已给用户确认）
- [x] **步骤 5**：示例文件完整流程走查 + 交付（e2e 测试覆盖：加载→抽道集→标注→保存→续标→返回修改）
- [x] **步骤 6**：web_app.py Gradio Web 界面（gradio 6.25 装于项目 `_vendor/`；端到端 API 测试通过；服务运行于 0.0.0.0:7860）

## 6. 待用户拍板的开放项

1. 初版标签清单中新增的「随机噪声、工业噪声」及每项的「不确定」选项 —— 待确认/增删
2. 句子模板措辞 —— 可现在定，也可之后改配置文件

## 7. 变更记录

| 日期 | 变更 |
|---|---|
| v0.1 | 初版计划建立（需求确认完成、示例文件诊断完成） |
| v0.2~v0.4 | 读取层、抽道集/预处理/出图、标签体系与存储依次完成（详见 MEMORY.md） |
| v1.0 | 全部 5 步完成：GUI 整合 + 端到端走查通过，首个可用版本 |
| v1.0.1 | README 补充远程无显示服务器运行方案（VNC 插件 / X11 转发） |
| v1.1 | 新增 Web 版 web_app.py（Gradio，装于 _vendor/），服务 0.0.0.0:7860 |
| v1.2 | 导出图片改为纯数据图：无坐标轴/标题，固定宽512×高1024（render_data_only） |
| v1.3 | 标签体系改版为 10 特征（见 label_config.yaml），去除「不确定」；新增「继承上一张」按钮 |
| v1.4 | gather_id 加入来源文件名前缀，多文件可合并标注到同一输出目录互不覆盖 |
| v1.5 | 标注区排版优化：左图右栏、特征分两列、按钮沉底 |
| v1.6 | 保存时同步存原始数据 npy（npy/ 子目录）；backfill_npy.py 回补 158 条历史记录 |
| v1.7 | clip 随机采样增强保存：范围+张数可配，每张增强图一条独立记录，重复保存自动清理旧增强 |
| v1.8 | backfill_augment.py 回补 158 条（01: 95~99.9，02/03: 90~99.9，各5张）；clip 采样改无放回去重 |
| v1.9 | 标签体系删除「线性噪声」「多次波」（10→8 特征）；migrate_remove_features.py 同步清理 948 条旧记录 |
| v2.0 | 面波/近炮点强能量噪声新增矩形框选标注（Web 版）：JSONL 加 regions 字段，强制画框校验，导出图保持干净 |
| v2.1 | 继承改为回找最近已标注道集（不继承框）；剔除功能曾加入后按用户决定移除；清空 output/ 旧标注（948 条）重标 |
| v3.0 | 多人协作中央服务：jobmanager 作业/任务池/租约、users 账号/角色、cloudsync(COS)、web_app 去全局单例改多人（2026-09-08；spec/plan 见 docs/superpowers） |
