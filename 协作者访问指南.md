# 地震道集标注器 —— 协作者访问指南

通过 **Tailscale（虚拟局域网）** 安全地访问主机的标注服务，适用于不在同一局域网（例如校园网不同网段）的协作标注。

> 只有加入了同一个 Tailscale 网络（tailnet）的机器才能访问；标注系统本身还有一层账号密码，双层保护。

---

## 一、安装 Tailscale

**Windows 用户**：

- 下载安装：<https://tailscale.com/download>
- 或管理员 PowerShell 执行：`winget install Tailscale.Tailscale`

**macOS 用户**：<https://tailscale.com/download>

**Linux 用户**：见 <https://tailscale.com/download/linux>（`curl -fsSL https://tailscale.com/install.sh | sh`）

安装完成后打开/运行 Tailscale，**先不用自己建网络**，进入第二步直接用邀请链接加入即可。

---

## 二、点击邀请链接加入我的网络

用浏览器打开下面这个邀请链接：

```
https://login.tailscale.com/uinv/iTxokw4pCe1187c59UK1311
```

- 如果还没有账号，用你自己的 **微软 / Google / GitHub / 邮箱** 注册一个即可；
- 登录后按提示 **接受邀请 / Accept**，会自动加入我的 Tailscale 网络；
- 确认已加入：终端执行 `tailscale status`，应能看到网络里的设备列表。

> 请选择ZDDWLIG@github加入。

---

## 三、访问标注服务

加入成功后，用浏览器打开**主机**的地址：

```
http://100.113.198.113:7860
```

打开后出现登录框，输入**标注账号和密码**（请联系管理员分配），即可开始标注。

---

## 使用流程（简要）

1. 选作业 → 点 **领取下一张**；
2. 按左侧特征逐项选择、需要时画框；
3. 点 **保存并释放**，会自动领取下一张；
4. 右上角可 **登出 / 切换账号**。

---

## 常见问题

**Q1：打开 `http://100.113.198.113:7860` 打不开 / 转圈？**
- 确认你的 Tailscale 已登录且在线：终端 `tailscale status`；
- 确认主机电脑**开机、Tailscale 在线、且标注服务正在运行**（服务由管理员在主机上启动）；
- 确认你已加入邀请链接对应的网络。

**Q2：提示要求登录？**
正常，用管理员分配的标注账号登录即可（不是 Tailscale 账号）。

**Q3：能访问但很卡 / 图加载慢？**
可能是主机到你的中继较慢，属正常现象；也可向管理员反馈。

---

> 备注：局域网内（与主机同一 192.168.1.x 网段）的设备可直接访问 `http://192.168.1.121:7860`，无需 Tailscale。
