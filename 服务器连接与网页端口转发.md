# 标注网页连接指南

每位协作者使用自己的服务器账号连接，无需使用或获取 `geoeast` 的密码。

## 1. 使用 Cursor/VS Code 连接服务器

按 `Ctrl+Shift+P`，执行：

```text
Remote-SSH: Connect to Host...
```

选择已经配置好的服务器，并使用自己的服务器账号登录。

SSH 配置示例：

```sshconfig
Host 4090-6004
    HostName 39.97.249.139
    Port 6004
    User gaotianxiang
```

## 2. 转发标注网页

连接服务器后：

1. 按 `Ctrl+Shift+P`。
2. 执行 `Ports: Focus on Ports View`。
3. 在底部的 **Ports** 面板点击 **Forward a Port**。
4. 输入远程端口：

   ```text
   7860
   ```

5. 点击 Ports 面板显示的本地地址，在浏览器中打开网页。

本地地址通常是：

```text
http://127.0.0.1:7860/
```

如果编辑器自动分配了其他本地端口，以 Ports 面板显示的 **Forwarded Address** 为准。

## 3. 登录网页

使用分配给自己的网页标注账号登录。服务器 SSH 账号和网页标注账号相互独立，请勿共享管理员 `boss` 账号。
